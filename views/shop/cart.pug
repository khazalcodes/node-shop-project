extends ../layouts/main-layout.pug 

block styles
	link(rel="stylesheet", href="/css/product.css")
	link(rel="stylesheet", href="/css/forms.css")

block content
	main
		if hasCartLines
			ul
				each cartLine, id in cartLines
					li
						p #{cartLine.productTitle}: (#{cartLine.quantity} x $#{cartLine.unitPrice}) $#{cartLine.totalPrice}
						form.button-form(action='/shop/cart/remove-product', method='post')
							button.btn.btn-warning(type='submit') Remove from Cart
							input(type='hidden' name='id' value=id)

			h2 Total price: #{sumTotal}

